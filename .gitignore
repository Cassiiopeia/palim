# 환경 변수 · 인증 정보 (절대 커밋 금지)
.env
.env.*
!.env.example
*.pem
*.key
secrets/
credentials/

# 채널 API 인증 정보
channel-credentials.json
config.local.*

# 의존성
node_modules/
# package-lock.json 은 커밋한다 — 없으면 CI 가 매번 최신 버전을 받아 빌드가 불안정해진다.

# 빌드 산출 CSS — Gradle 이 생성한다. 커밋하지 않는다.
palim-web/src/main/resources/static/css/
__pycache__/
*.py[cod]
.venv/
venv/

# 빌드 산출물
dist/
build/
*.egg-info/
target/
out/

# Gradle
.gradle/
!gradle/wrapper/gradle-wrapper.jar
!gradle/wrapper/gradle-wrapper.properties

# Spring Initializr
HELP.md
*.zip

# 로그
logs/
*.log
npm-debug.log*

# 데이터베이스 · 데이터
*.sqlite
*.db
data/
pgdata/
dumps/

# 업로드 파일 (롯데백화점 엑셀 등)
uploads/
*.xlsx
*.xls
!docs/**/*.xlsx

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
desktop.ini

# 테스트 · 커버리지
coverage/
.coverage
.pytest_cache/
htmlcov/

# 사내 Redmine·pad 참고 자료 (내부 캡처 포함) — PUBLIC 레포이므로 절대 커밋하지 않는다
docs/somansa/

# 운영·개발 설정 — 실제 값은 환경변수/Secrets 로 주입한다. 공개 레포에 커밋하지 않는다.
# 형태는 *.example 참조. application.yaml 만 공개 커밋한다(placeholder 만).
application-prod.yaml
application-prod.yml
application-dev.yaml
application-dev.yml
