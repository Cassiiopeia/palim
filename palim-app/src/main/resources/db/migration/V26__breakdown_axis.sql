-- 합계를 **무엇을 기준으로 뜯어볼지**를 대조마다 기억한다.
--
-- 처음에는 「품명이 닮은 것끼리」 하나로 못박아 두었다. 어느 발주사의 자료에서는 로트가 품명
-- 안에 들어 있어 그것만으로 잘 맞았기 때문인데, 그건 **그 자료의 사정**이지 이 프로그램의
-- 성질이 아니다. 로트를 별도 칸으로 주는 시스템, 창고별로 갈라 봐야 하는 회사, 품목코드를
-- 양쪽이 똑같이 쓰는 곳 — 전부 다른 기준이 필요하고, 기준이 코드에 박혀 있으면 그런 곳에서는
-- 이 화면이 통째로 쓸모없어진다.
--
-- 고를 수 있는 값은 **표준 모델의 칸 목록**에서 나온다. 칸이 늘면 고를 것도 저절로 는다.
-- 기본값은 어디서나 일단 도는 「품명이 닮은 것끼리」다.
ALTER TABLE reconcile_definition
    ADD COLUMN breakdown_axis varchar(100) NOT NULL DEFAULT 'NAME';
