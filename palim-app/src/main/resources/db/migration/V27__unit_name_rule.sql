-- 여러 품목을 묶을 때 **그 묶음을 뭐라 부를지**를 대조마다 기억한다.
--
-- 처음에는 첫 품목의 이름을 그대로 썼다. 그래서 로트 넷을 묶은 묶음이
-- 「클래식 850g (27.03.16)」 이 되었다 — 로트 하나의 날짜가 묶음 전체를 대표한 것이다.
-- 공통 부분만 쓰도록 고쳤는데, 그것도 **모든 자료에 옳지는 않다.** 품명이 아예 다른 두
-- 시스템에서는 공통 부분이 토막으로 남고, 그때는 첫 이름을 그대로 두는 편이 낫다.
--
-- 그래서 코드가 정하지 않고 고를 수 있게 둔다. 어느 쪽도 안 맞으면 「직접만」 을 골라
-- 사람이 매번 짓는다.
ALTER TABLE reconcile_definition
    ADD COLUMN unit_name_rule varchar(30) NOT NULL DEFAULT 'COMMON';
