/**
 * 감사 로그 도메인.
 *
 * <p>누가 · 언제 · 어디서 · 무엇을 했는지를 기록한다. 인증(로그인·로그아웃·실패), 조회, 변경을
 * 모두 남긴다.
 *
 * <h2>다른 도메인을 의존하지 않는다</h2>
 *
 * <p>모든 도메인이 이 모듈을 호출하므로, 이 모듈이 어떤 도메인이라도 의존하면 순환이 생긴다.
 * actor 와 대상은 문자열 값으로만 받는다.
 *
 * <h2>HTTP 를 모른다</h2>
 *
 * <p>IP · User-Agent · 요청 URI 는 값으로 받는다. {@code HttpServletRequest} 에서 뽑는 일은
 * {@code palim-web} 의 책임이다. 그래야 배치나 스케줄러에서도 감사 기록을 남길 수 있다.
 */
package kr.suhsaechan.palim.audit;
