/**
 * 채널 도메인.
 *
 * <p>채널 설정, API 인증정보, 수집 커서와 함께 각 채널의 공식 API를 호출하는 어댑터를 소유한다.
 *
 * <p>어댑터는 채널별 인증·응답 형식 차이를 흡수해 공통 {@code record}로 반환한다. 이를
 * 주문 엔티티로 변환하는 책임은 {@code palim-collector}에 있으므로, 이 모듈은 주문·재고
 * 도메인을 알지 못한다. 신규 채널 추가가 기존 코드에 영향을 주지 않는 이유다.
 *
 * <p>인증정보는 AES-GCM으로 암호화해 저장하고 마스터키는 환경변수로 주입한다(설계서 6.2).
 * 호출 제한은 채널별 RateLimiter로 준수하며, 쿠팡은 지속 초과 시 영구 차단되므로 임계
 * 도달 시 수집을 자동 중단한다(설계서 6.1).
 */
package kr.suhsaechan.palim.channel;
