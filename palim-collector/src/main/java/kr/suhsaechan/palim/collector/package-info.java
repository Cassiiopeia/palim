/**
 * 수집 조율 계층.
 *
 * <p>채널별 수집 스케줄러와 주문 수집 조율 트랜잭션을 소유한다. 도메인 모듈이 아니라
 * <b>여러 도메인을 관통하는 책임</b>을 갖는 계층이다.
 *
 * <p>주문 수집 1건은 매핑 조회 → 주문 저장 → 재고 차감 → Outbox 삽입으로 도메인 4개를
 * 관통하며, 이 전체가 한 트랜잭션이어야 한다. <b>트랜잭션을 여는 것은 이 계층뿐이다.</b>
 * 도메인 서비스가 각자 트랜잭션을 열면 재고는 차감됐는데 주문은 롤백되는 상태가 발생한다.
 *
 * <p>수집 커서는 구간을 겹쳐서 조회한다. 채널 API는 주문 시각이 지연 반영되는 경우가 있어
 * 구간을 정확히 이어붙이면 경계에서 주문이 누락되는데, 중복은 유니크 제약이 막지만 누락은
 * 아무도 감지하지 못하기 때문이다(설계서 5.4).
 */
package kr.suhsaechan.palim.collector;
