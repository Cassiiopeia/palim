/**
 * SKU · 재고 도메인.
 *
 * <p>SKU, 재고 수량, 안전재고 임계치, 재고 변동 이력을 소유한다. 어느 채널에서 판매되든
 * 동일한 재고에서 차감되는 단일 기준이다(F-03).
 *
 * <p>다른 도메인 모듈을 의존하지 않는다. 채널·주문·매핑을 알지 못하며, 외부에서 넘겨받은
 * SKU 식별자와 수량만으로 동작한다. 재고 차감은 비관적 락으로 보호하되 트랜잭션은
 * 호출자({@code palim-collector})가 연다.
 */
package kr.suhsaechan.palim.sku;
