/**
 * 관리자 화면 계층.
 *
 * <p>Thymeleaf 컨트롤러, 템플릿, Security 설정, 화면용 조회를 소유한다. 도메인 모듈이 아니라
 * 여러 도메인을 조율하는 계층이며, {@code palim-collector}와는 목적이 달라 서로를 의존하지 않는다.
 *
 * <p>여러 도메인에 걸친 조회는 JPA로 조인할 수 없으므로 {@code JdbcClient}로 직접 SQL을 쓰고
 * 결과를 {@code record}로 매핑한다. 쓰기는 도메인 모듈(JPA), 읽기는 SQL로 나뉘며 이는 설계서
 * 4.4가 규정한 조회 계층 분리를 충족한다.
 *
 * <p>별도 모바일 앱은 만들지 않는다. 알림 수신은 텔레그램이 담당하므로 앱의 푸시 기능이
 * 중복되고, 설정 변경은 빈도가 낮아 전용 앱의 필요성이 낮다(F-09).
 */
package kr.suhsaechan.palim.web;
